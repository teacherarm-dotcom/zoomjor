import Foundation

/// ชื่อ/แบรนด์ของโปรแกรม รวมไว้ที่เดียว
enum AppInfo {
    static let name = "ZoomJor"
    static let thaiName = "ซูมจอ"
    static let tagline = "ซูมจอ • วาดบนจอ • จับเวลาพัก"
    static let fileprefix = "ZoomJor"     // ชื่อนำหน้าไฟล์ภาพที่เซฟ
}

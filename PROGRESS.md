# ZoomJor (ซูมจอ) — บันทึกความคืบหน้า

แอป macOS บนแถบเมนู ช่วยนำเสนอ/สอน: ซูมหน้าจอ · วาดทับหน้าจอ · ซูมสด · นาฬิกาจับเวลาพัก
Swift/AppKit ล้วน ไม่มี dependency
repo: https://github.com/teacherarm-dotcom/zoomjor (public)

- **2026-09-06 (1)** — สร้างโปรแกรมครั้งแรกในชื่อ `ZoomIt` (เลียนแบบ ZoomIt ของ Sysinternals) · โหมด `⌃1` ซูม (แช่จอ) + `⌃2` วาดทับจอสด · เครื่องมือ ลูกศร/ปากกา/เส้น/สี่เหลี่ยม/วงรี/ไฮไลต์/ข้อความ · สี 8 สี (แดง เขียว เหลือง ดำ น้ำเงิน + ขาว ส้ม ชมพู) · จับภาพด้วย ScreenCaptureKit ความละเอียด Retina เต็ม · คีย์ลัดระบบใช้ Carbon `RegisterEventHotKey` (ไม่ต้องขอสิทธิ์ Accessibility) · verify: เรนเดอร์ทดสอบลูกศรครบ 5 สี + ซูม 3× ภาพวาดเกาะตำแหน่งเดิม
- **2026-09-06 (2)** — เพิ่ม **โหมดนาฬิกาจับเวลาถอยหลัง `⌃3`** (พื้นหลังภาพจอเบลอ+หรี่ · วงแหวนนับถอยหลังเปลี่ยนสีเขียว→ส้ม(<1 นาที)→แดงกะพริบ+เสียง Glass.aiff 3 ครั้ง · บอกเวลากลับมาต่อ · Space หยุด/เดินต่อ · ↑↓ ±1 นาที · ←→ ±10 วิ · พิมพ์ตัวเลข+Enter ตั้งเวลา · ⌘R เริ่มใหม่ · ตั้งเวลาเริ่มต้นจากเมนู จำใน UserDefaults) และ **โหมดซูมสด Live zoom `⌃4`** (SCStream 60fps · `ignoresMouseEvents` คลิกทะลุใช้แอปข้างล่างต่อได้ · ยึดจุดใต้เคอร์เซอร์ `origin = cursor × (1 − 1/zoom)` คลิกตรงไหนโดนตรงนั้น · แถบควบคุมลอย `NSPanel(.nonactivatingPanel)` ไม่แย่งโฟกัส · คีย์ ⌃⌥↑/↓/0 จองชั่วคราวแล้วถอนคืนตอนปิด) · verify: เรนเดอร์ทดสอบนาฬิกา 3 สถานะ + พิสูจน์คณิตศาสตร์ยึดจุด (กากบาทที่ 3× อยู่ตำแหน่งเดิมเป๊ะ)
- **2026-09-06 (3)** — **เปลี่ยนชื่อเป็น ZoomJor (ซูมจอ)** ตามที่เจ้าของเลือก · bundle id `net.kruarm.zoomjor` · ย้ายโฟลเดอร์ `zoomit-mac` → `zoomjor` · เพิ่ม `AppInfo.swift` (ชื่อ/แบรนด์รวมที่เดียว) + `make-icon.sh` (PNG 1024 → .icns) + ช่องใส่ `Resources/MenuBarIcon.pdf`
- **2026-09-06 (4)** — 🐛 **แก้บั๊กจริง: คีย์ลัดกดไม่ได้ตอนสลับแป้นเป็นภาษาไทย** (เจ้าของแจ้ง) — เดิมอ่านคีย์จาก `charactersIgnoringModifiers` ซึ่งแป้นไทยคืนอักษรไทย ทำให้ `⌘Z`/`u`/สี/เครื่องมือ ใช้ไม่ได้เลย · **แก้เป็นอ่าน `event.keyCode` (ตำแหน่งปุ่มจริง) ทั้งหมด** — `Tool.keyCode` / `PaletteColor.keyCode` / `DigitKeys.map` ใน `Annotation.swift` (ยกเว้นตอนพิมพ์ข้อความยังใช้ `event.characters` เพื่อพิมพ์ไทยได้)
  · 🐛 **แก้บั๊กจริงข้อ 2 (เจอตอนเขียนเทสต์)**: undo เก็บทีละเส้น ทำให้ "ลบทั้งหมด" ย้อนกลับไม่ได้ ทั้งที่ข้อความบนจอบอกให้กด ⌘Z · **เปลี่ยนเป็น undo แบบสแนปช็อตของการกระทำ** (`undoStack: [[Stroke]]` ลึก 300 ขั้น) ครอบคลุมวาดเส้น/ใส่ข้อความ/ลบทั้งหมด
  · เพิ่ม **แถบเครื่องมือคลิกได้บนจอ** (เครื่องมือ 7 + สี 8 + ย้อนกลับ/ทำซ้ำ/ลบทั้งหมด + ออก · SF Symbols ย้อมขาว มี fallback เป็นตัวอักษร · ชี้ค้างขึ้นชื่อปุ่ม · กด `D` ซ่อน) — ใช้งานได้โดยไม่ต้องจำคีย์เลย
  · เพิ่มทาง undo: `⌫` Backspace (เดิมเป็น "ลบทั้งหมด" ย้ายไป `E` / `⌘⌫`)
  · ใส่ไอคอนแอปจาก `zoomjor icon.png` (1254px → AppIcon.icns) · ลบ `/Applications/ZoomIt.app` เก่าทิ้ง
  · **verify: เทสต์อัตโนมัติ 30/30 ผ่าน** (`rt3`) — จำลอง `NSEvent` ที่ `characters = "ก"` ทุกครั้งเพื่อยืนยันว่าใช้ได้จริงตอนแป้นไทย + จำลองลากเมาส์วาด/undo/redo/clear/คลิกปุ่มบนแถบ · เรนเดอร์ภาพแถบเครื่องมือตรวจตาแล้ว

- **2026-09-06 (5)** — `git init` + push ขึ้น GitHub เป็น **public repo `teacherarm-dotcom/zoomjor`** (คอมมิตแรก `2c2c6cd` 17 ไฟล์) · `.gitignore` ตัด `ZoomJor.app/` (build artifact) และ `.DS_Store` · เก็บ `Resources/AppIcon.icns` + `zoomjor icon.png` ไว้ใน repo เพื่อให้ `./build.sh` ใช้ได้ทันทีหลัง clone · ใส่ topics ให้ค้นเจอ

- **2026-09-06 (6)** — เพิ่ม `LICENSE` แบบ **MIT** (© 2026 Thanasarn Rujira ตาม `git config user.name`) + หัวข้อสัญญาอนุญาตใน README · GitHub จะขึ้นป้าย MIT ให้เอง

- **2026-09-07** — ✅ **ยืนยันการติดตั้งบนเครื่องจริง**: บิลด์ใหม่จากซอร์สล่าสุด ได้ไบนารี sha256 ตรงกับตัวที่ติดตั้งอยู่เป๊ะ (`d44ecb11…`) จึงไม่เขียนทับ — สิทธิ์ Screen Recording เดิมไม่ถูกรีเซ็ต · แอปรันอยู่ที่ `/Applications/ZoomJor.app` (bundle id `net.kruarm.zoomjor`) · ยืนยันครบ 4 โหมดในซอร์ส: `⌃1` ซูมแช่จอ · `⌃2` วาดทับจอสด · `⌃3` นาฬิกาถอยหลัง (ค่าเริ่มต้น 10 นาที เก็บใน UserDefaults) · `⌃4` ซูมสด
  · 📄 เพิ่ม **คู่มือใช้งาน 1 หน้า** `docs/guide.html` (คีย์ลัดครบทุกโหมด ตรวจตรงกับโค้ดจริงทุกปุ่ม · สีทั้ง 8 ใส่ค่า hex จริงจาก `Annotation.swift` · ปริ้นต์ได้ · รองรับธีมสว่าง/มืด) + ลิงก์จาก README · เผยแพร่เป็นหน้าเว็บ: https://claude.ai/code/artifact/80fe30b0-c9d1-489e-a02d-aef99bbde2b0
  · 📌 จุดที่คู่มือระบุไว้ให้ชัด (คนใช้มักงง): โหมด `⌃4` **กด Esc ออกไม่ได้** เพราะคีย์บอร์ดถูกส่งต่อให้แอปข้างล่าง — ต้องกด `⌃4` ซ้ำ หรือปุ่ม ✕ บนแถบลอย · ในโหมดนาฬิกาแถบเครื่องมือถูกซ่อนไว้ (`hudHidden = mode == .timer`) กด `D` เรียกขึ้นมา

- **2026-09-07 (2)** — 🐛 **แก้บั๊กจริง: `⌃4` ซูมสดขึ้นจอดำสนิท** (เจ้าของแจ้ง) — ต้นเหตุ: หน้าต่างซูมสดทึบดำคลุมทั้งจอ แต่ไม่ได้กันตัวเองจากการถูกจับภาพ ถ้า `SCContentFilter(excludingApplications:)` ตัดไม่ติด (แอปเป็น `LSUIElement` หน้าต่าง borderless ที่ระดับ shielding อาจไม่ถูกนับใน `content.applications`) จะจับหน้าต่างดำของตัวเองมาแสดงวน = ดำถาวร
  · **แก้ 4 จุด**: (1) `window.sharingType = .none` + panel ด้วย — บังคับให้ระบบไม่จับหน้าต่างซูมสดเลย ⚠️ ผลข้างเคียงที่ยอมรับ: ตอนแชร์หน้าจอ (Zoom/Meet) ปลายทางจะไม่เห็นภาพขยายของ `⌃4` — ให้ใช้ `⌃1` แทน (ไม่ตั้ง `.none` เพราะลายเส้นต้องแชร์ออกไปได้) (2) fallback ของ filter เดิมเป็น `excludingWindows([])` = ไม่ตัดอะไรเลย → เปลี่ยนเป็นตัดหน้าต่างของ pid ตัวเอง (3) เลิกใช้ `VTCreateCGImageFromCVPixelBuffer` ที่ห่อ CVPixelBuffer ตรง ๆ (SCStream หมุนใช้ buffer ซ้ำ ภาพเพี้ยน/ดำได้) → `CIContext.createCGImage` ซึ่งคัดลอกพิกเซลจริง (4) ถอด `import VideoToolbox`
  · 🔎 **เพิ่ม `ZLog` → `~/Library/Logs/ZoomJor.log`** (จำนวนแอป/หน้าต่างที่เห็น · เลือก filter แบบไหน · ขนาดเฟรมแรก · เฟรมที่ถูกข้ามพร้อม status · error) — ปัญหาแบบนี้ดูจาก debugger ไม่ได้เพราะเกิดเฉพาะตอนรันบนหน้าจอจริง
  · 🖥️ **จอดำต้องไม่เงียบอีก**: `LiveZoomView` แสดงข้อความ “กำลังเริ่มภาพสด…” และถ้าเกิน 2 วินาทียังไม่ได้เฟรมแรก จะขึ้นวิธีแก้ (เปิดสิทธิ์บันทึกหน้าจอ) กลางจอ
  · 📄 คู่มือ `docs/guide.html` เพิ่ม 2 หมายเหตุ: ซูมสดกับการแชร์หน้าจอ · ตำแหน่งไฟล์ log
  · ⚠️ ไบนารีเปลี่ยน = ลายเซ็นเปลี่ยน → **ต้องอนุญาต Screen Recording ใหม่**

- **2026-09-07 (3)** — ✅ **ยืนยันจาก log ว่า `⌃4` หายดำแล้ว** และพิสูจน์ว่าวินิจฉัยถูก: รอบ 07:21 `myWindows=2` ตัดแอปตัวเองติด · **รอบ 07:26 `myWindows=0` → `self NOT in applications list`** (เคสที่ทำให้ดำเดิม ๆ) แต่ภาพยังมาปกติเพราะ `sharingType = .none` กันไว้ — ทั้งสองรอบได้เฟรมแรก 3024x1964 และแสดงผลได้
  · 🎨 **เจ้าของสั่งเปลี่ยนโหมดวาดเริ่มต้น**: ค่าเริ่มต้นเป็น **ปากกา** (เดิมลูกศร) · **`⇧` ค้างขณะลาก = ลูกศร** · **`⌃⇧` ค้าง = สี่เหลี่ยม** — เป็นทางลัดชั่วคราว ไม่เปลี่ยนเครื่องมือที่เลือกค้างไว้ และสลับ ลูกศร↔สี่เหลี่ยม กลางคันได้
  · ⚠️ **ย้ายปุ่มบังคับมุม 45°/จัตุรัส จาก `⇧` ไปเป็น `⌥`** เพราะ `⇧` ถูกใช้เป็นทางลัดลูกศรแล้ว
  · 🐛 **กันเคส `⌃`+คลิก ที่ macOS อาจส่งมาเป็นคลิกขวา**: เดิม `rightMouseDown` = ออกจากโหมดทันที → `⌃⇧`+ลาก อาจหลุดออกแทนที่จะวาด · แก้เป็น ถ้ากด `⇧` ค้างให้เข้าทางวาด + เพิ่ม `rightMouseDragged/Up` (รีแฟกเตอร์เป็น `beginStroke/continueStroke/endStroke` ใช้ร่วมทั้งซ้ายและขวา) · คลิกขวาเปล่า ๆ ยังออกเหมือนเดิม
  · ✅ **เทสต์อัตโนมัติ 18/18 ผ่าน** — เก็บถาวรที่ `Tests/main.swift` รันด้วย `./Tests/run.sh` (จำลอง NSEvent ลากเมาส์: ปากกาเริ่มต้น · ⇧ ลูกศร · ⌃⇧ สี่เหลี่ยม · คลิกขวา+⇧ ยังวาดได้ · คลิกขวาเปล่าไม่วาด · สลับกลางคัน · ⌥ สแนป Δy≈0 · ไม่กด ⌥ ไม่สแนป · ทางลัดใช้ได้แม้เลือกไฮไลต์/ข้อความไว้ · กล่องข้อความว่างไม่ค้าง)

- **2026-09-10** — 🐛 **แก้บั๊กจริง: ไอคอนบนแถบเมนูไม่ขึ้น → ผู้ใช้ปิดโปรแกรมไม่ได้เลย** (เจ้าของแจ้งพร้อมภาพแถบเมนู) · แอปเป็น `LSUIElement` ไม่มีไอคอน Dock/หน้าต่าง ⇒ ไม่มีไอคอน = ไม่มีทางออกจากโปรแกรม
  · 🔎 **หาสาเหตุด้วยหลักฐาน ไม่เดา**: ทดสอบ `NSImage(systemSymbolName:)` แยกต่างหาก → โหลดได้ปกติ (16×15) · ไม่มีแอปจัดการแถบเมนู (Bartender/Ice) รันอยู่ · แต่มีแอป `LSUIElement` รันพร้อมกัน ~29 ตัว (FortiClient, Logi Tune, OneDrive, Snap Camera, Webex, LINE, Notion, zoom.us, Windows App…)
  · ✅ **ยืนยันด้วย log จากแอปจริง**: `statusItem: frame=(534.0, 949.0, 32.0, 33.0) visible=true ซ่อนอยู่=true` — ไอคอนถูกสร้างจริงและ `isVisible=true` แต่ macOS วางไว้ที่ x=534 ซึ่ง**ไม่อยู่ใน `NSScreen.auxiliaryTopRightArea`** (จอมีรอยบาก ที่ทางฝั่งขวาเต็ม) ⇒ ถูกบังจนกดไม่ได้ · **ไม่มี API บังคับลำดับความสำคัญ** แก้ที่ตัวแอปไม่ได้ ต้องให้ทางออกอื่น
  · 🔑 **เพิ่มคีย์ลัดสำรองที่ไม่ต้องพึ่งไอคอน**: `⌃⌥M` เปิดเมนูตรงตำแหน่งเมาส์ (`NSMenu.popUp` พิกัดจอ) · `⌃⌥Q` ปิดโปรแกรม
  · 🖥️ **ตัวตรวจอัตโนมัติ `checkStatusItemPlacement()`** (หน่วง 2 วิ ให้ระบบจัดตำแหน่งเสร็จ) เทียบ frame กับ `auxiliaryTopRightArea` → ถ้าถูกซ่อนเด้ง alert บอกคีย์ลัดสำรอง + วิธีเรียกไอคอนกลับ (แสดงครั้งเดียว เก็บธง `warnedIconHidden` ใน UserDefaults)
  · 🛟 **fallback ตัวอักษร `ZJ`** เมื่อ `menuBarIcon()` คืน nil — เดิมจะได้ปุ่มกว้าง 0 กดไม่โดน หาไม่เจอทั้งที่แอปรันอยู่
  · 📄 คู่มือ `docs/guide.html` เพิ่มหัวข้อ “ถ้าไอคอนไม่ขึ้นบนแถบเมนู” + `⌃⌥Q`/`⌃⌥M` ในตารางคีย์ลัด

## ⚠️ ข้อควรรู้

- เปลี่ยน bundle id เมื่อไหร่ **สิทธิ์ Screen Recording ต้องอนุญาตใหม่** (TCC ผูกกับ bundle id + ลายเซ็น)
- แอปเซ็นแบบ ad-hoc (`codesign -s -`) — ใช้เองบนเครื่องนี้ได้ ถ้าจะแจกต้อง notarize
- ยังไม่ได้ทดสอบบนหน้าจอจริง 2 อย่าง: สตรีมภาพ Live zoom และแถบควบคุมลอย (โปรเซสที่ใช้พัฒนาไม่มีสิทธิ์บันทึกหน้าจอ)
- ทำงานทีละจอ (จอที่เมาส์อยู่ตอนกดคีย์ลัด)
